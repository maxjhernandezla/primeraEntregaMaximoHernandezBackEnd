<div>
  {{!-- <div class="userData">
    <h1>Bienvenido {{user.name}}</h1>
    <p>Email: {{user.email}}</p>
    <p>Role: {{user.role}}</p>
    <a href="/api/sessions/logout"><button>Cerrar sesión</button></a>
  </div> --}}
  <div class="generalContainer">
    {{#each users}}
      <div class="usersContainer">
        <div class="userData">
          <p>{{this.name}}</p>
          <p>{{this.role}}</p>
          <p>Change role:</p>
          <input type="text" name="role">
        </div>
        <div class="bnContainer">
          {{!-- <button class="addBtn" data-pid={{this._id}}>Delete user</button> --}}
        </div>
      </div>
    {{/each}}
  </div>
</div>
<script src="/js/admin.js"></script>